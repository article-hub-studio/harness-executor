{
  "name": "upio-mcp-executor-harness",
  "version": "1.3.1",
  "description": "Harness Executor — mobile-first WebUI theo phong cách OpenCode + zero-dependency Node backend: registry Luau/LSP với 10 MCP server THẬT (không mô phỏng), 10 plugin có behavior thật, 13 skill, MCP executor tự bật khi boot, environment auto-builder, custom model hub và điều phối đa-agent.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "generate": "node scripts/generate-registries.js",
    "icons": "node scripts/gen-icons.js",
    "build-icons": "node scripts/build-icons.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "keywords": [
    "mcp",
    "executor",
    "harness",
    "mobile",
    "pwa",
    "agents",
    "model-hub"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
